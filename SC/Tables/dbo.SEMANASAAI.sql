SET ANSI_NULLS ON
SET QUOTED_IDENTIFIER ON
SET ANSI_PADDING OFF
GO
CREATE TABLE [dbo].[SEMANASAAI] (
		[SEM_CODIGO]       [int] IDENTITY(1, 1) NOT NULL,
		[SEM_SEMANA]       [smallint] NOT NULL,
		[SEM_YEAR]         [smallint] NOT NULL,
		[SEM_FECHAINI]     [datetime] NOT NULL,
		[SEM_FECHAFIN]     [datetime] NOT NULL,
		CONSTRAINT [IX_SEMANASAAI]
		UNIQUE
		NONCLUSTERED
		([SEM_CODIGO])
		ON [PRIMARY]
) ON [PRIMARY]
GO
ALTER TABLE [dbo].[SEMANASAAI]
	ADD
	CONSTRAINT [PK_SEMANASAAI]
	PRIMARY KEY
	CLUSTERED
	([SEM_SEMANA], [SEM_YEAR])
	ON [PRIMARY]
GO
