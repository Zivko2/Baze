SET ANSI_NULLS ON
SET QUOTED_IDENTIFIER ON
GO




































CREATE FUNCTION NumberToText (@valor decimal(38,6),@mask int)  
RETURNS varchar(8000) AS  
BEGIN 
	RETURN (select convert(varchar(8000),isnull(@valor,0),@mask));
END




























































GO
