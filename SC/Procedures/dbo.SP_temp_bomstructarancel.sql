SET ANSI_NULLS ON
SET QUOTED_IDENTIFIER ON
GO




























CREATE PROCEDURE [dbo].[SP_temp_bomstructarancel]   as


/*

UPDATE dbo.BOM_STRUCT
SET     dbo.BOM_STRUCT.AR_CODIGO= dbo.MAESTRO.AR_IMPFO
FROM         dbo.BOM_STRUCT INNER JOIN
                      dbo.MAESTRO ON dbo.BOM_STRUCT.BST_HIJO = dbo.MAESTRO.MA_CODIGO
WHERE dbo.BOM_STRUCT.AR_CODIGO IS NULL

*/



























GO
