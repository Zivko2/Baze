SET ANSI_NULLS ON
SET QUOTED_IDENTIFIER ON
GO
































































CREATE VIEW dbo.VFE_ESTATUS
with encryption as
SELECT     CB_KEYFIELD, CB_LOOKUP
FROM         dbo.COMBOBOXES
WHERE     (CB_FIELD = 'FE_ESTATUS') AND (CB_TABLA = 62)
































































GO
