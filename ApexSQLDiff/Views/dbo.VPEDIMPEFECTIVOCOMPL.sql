SET ANSI_NULLS ON
SET QUOTED_IDENTIFIER ON
GO






CREATE VIEW dbo.VPEDIMPEFECTIVOCOMPL
with encryption as
SELECT     SUM(ISNULL(PIT_CONTRIBTOTMN, 0)) AS PIT_CONTRIBTOTMN, PI_CODIGO
FROM         dbo.VPEDIMPCONTRIBUCIONCOMPL
GROUP BY PI_CODIGO








GO
