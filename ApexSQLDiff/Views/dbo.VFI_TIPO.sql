SET ANSI_NULLS ON
SET QUOTED_IDENTIFIER ON
GO

































































CREATE VIEW dbo.VFI_TIPO
with encryption as
SELECT     CB_KEYFIELD, CB_LOOKUP
FROM         dbo.COMBOBOXES
WHERE     (CB_FIELD = 'FI_TIPO') AND (CB_TABLA = 138)

































































GO
