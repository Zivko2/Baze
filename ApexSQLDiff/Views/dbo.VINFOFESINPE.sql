SET ANSI_NULLS ON
SET QUOTED_IDENTIFIER ON
GO







































CREATE VIEW dbo.[VINFOFESINPE]
with encryption as
SELECT COUNT(FE_CODIGO) AS COUNTFe_COdIGO, FE_TIPO
FROM FACTEXP
WHERE (FE_CANCELADO = 'N') AND (PI_CODIGO <= 0) AND (FE_TIPO <> 'T')
GROUP BY FE_TIPO








































































GO
