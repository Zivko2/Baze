SET ANSI_NULLS ON
SET QUOTED_IDENTIFIER ON
GO
































































CREATE VIEW dbo.VFED_TIP_ENS
with encryption as
SELECT     CB_KEYFIELD, CB_LOOKUP
FROM         dbo.COMBOBOXES
WHERE     (CB_FIELD = 'FED_TIP_ENS')
































































GO
