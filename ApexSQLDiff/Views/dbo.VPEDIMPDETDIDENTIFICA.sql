SET ANSI_NULLS ON
SET QUOTED_IDENTIFIER ON
GO






CREATE VIEW dbo.VPEDIMPDETDIDENTIFICA
with encryption as
SELECT     dbo.PEDIMPDET.PI_CODIGO, dbo.PEDIMPDET.PID_INDICED, dbo.PEDIMPDETIDENTIFICA.IDE_CODIGO, dbo.PEDIMPDETIDENTIFICA.PIID_DESC, 
                      dbo.PEDIMPDETIDENTIFICA.PIID_CODIGO
FROM         dbo.PEDIMPDETIDENTIFICA INNER JOIN
                      dbo.PEDIMPDET ON dbo.PEDIMPDETIDENTIFICA.PIB_INDICEB = dbo.PEDIMPDET.PIB_INDICEB
WHERE     (dbo.PEDIMPDETIDENTIFICA.PIID_TIPO = 'N')










































GO
