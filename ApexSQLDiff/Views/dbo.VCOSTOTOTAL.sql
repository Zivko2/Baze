SET ANSI_NULLS ON
SET QUOTED_IDENTIFIER ON
GO






























































CREATE VIEW dbo.VCOSTOTOTAL
with encryption as
SELECT     BST_PT, SUM(BST_MATNOORIG + BST_MATORIG) AS COSTO, COUNT(BST_HIJO) AS COUNTNOORIG
FROM         dbo.CLASIFICATLC
GROUP BY BST_PT


































































GO
