SET ANSI_NULLS ON
SET QUOTED_IDENTIFIER ON
GO



















































CREATE VIEW dbo.VMA_INV_GEN
with encryption as
SELECT     CB_KEYFIELD, CB_LOOKUP
FROM         dbo.COMBOBOXES
WHERE     (CB_FIELD = 'MA_INV_GEN') AND (CB_TABLA = 41)































































GO
