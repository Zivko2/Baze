SET ANSI_NULLS ON
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW dbo.vRVFDESPACHO
with encryption as
SELECT     CB_KEYFIELD, CB_LOOKUP, CB_LOOKUPENG
FROM         dbo.COMBOBOXES
WHERE     (CB_FIELD = 'rvf_despacho') AND (CB_TABLA = 228)




GO
