SET ANSI_NULLS ON
SET QUOTED_IDENTIFIER ON
GO
























































CREATE VIEW dbo.VFACTIMPPES_BRU
with encryption as
SELECT     SUM(ISNULL(FID_PES_BRU, 0)) AS FID_PES_BRU, FI_CODIGO
FROM         dbo.FACTIMPDET
GROUP BY FI_CODIGO

























































GO
