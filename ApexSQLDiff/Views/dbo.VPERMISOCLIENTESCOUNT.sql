SET ANSI_NULLS ON
SET QUOTED_IDENTIFIER ON
GO











CREATE VIEW dbo.VPERMISOCLIENTESCOUNT
with encryption as
SELECT     COUNT(*) AS PERMISOCLIENTES, PE_CODIGO, PCL_REGISTROTIPO
FROM         dbo.PERMISOCLIENTES
GROUP BY PE_CODIGO, PCL_REGISTROTIPO




GO
