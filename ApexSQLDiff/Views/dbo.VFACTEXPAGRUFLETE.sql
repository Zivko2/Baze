SET ANSI_NULLS ON
SET QUOTED_IDENTIFIER ON
GO





CREATE VIEW dbo.VFACTEXPAGRUFLETE
with encryption as
SELECT     SUM(FE_FLETE) AS FEA_FLETE, SUM(FE_SEGURO) AS FEA_SEGURO, SUM(FE_EMBALAJE) AS FEA_EMBALAJE, FE_FACTAGRU
FROM         VFACTEXPFLETE
GROUP BY FE_FACTAGRU

















GO
