SET ANSI_NULLS ON
SET QUOTED_IDENTIFIER ON
GO









































CREATE VIEW dbo.VFACTIMPPAISMAX
with encryption as
SELECT     MA_CODIGO, MAX(COUNTPAIS) AS maxCOUNTPAIS, PA_CODIGO
FROM         dbo.VFACTIMPPAIS
GROUP BY MA_CODIGO, PA_CODIGO















































































































GO
