SET ANSI_NULLS ON
SET QUOTED_IDENTIFIER ON
GO







CREATE VIEW dbo.VPEDIMPQTYCOM
with encryption as
SELECT     PI_CODIGO, SUM(ROUND(PID_CAN_GEN, 3)) AS PID_CAN_GEN
FROM         dbo.PEDIMPDET
GROUP BY PI_CODIGO






GO
