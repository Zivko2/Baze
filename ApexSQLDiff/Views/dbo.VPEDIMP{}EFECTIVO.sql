SET ANSI_NULLS ON
SET QUOTED_IDENTIFIER ON
GO







CREATE VIEW dbo.[VPEDIMP<>EFECTIVO]
with encryption as
SELECT     PI_CODIGO, SUM(ROUND(PIT_CONTRIBTOTMN, 0)) AS PIT_CONTRIBTOTMN
FROM         [VPEDIMP<>EFECTIVO1]
GROUP BY PI_CODIGO
























GO
