SET ANSI_NULLS ON
SET QUOTED_IDENTIFIER ON
GO


CREATE VIEW dbo.VPEDIMPPAISMAX
with encryption as
SELECT     PI_CODIGO, MA_GENERICO, MAX(COUNTPAIS) AS maxCOUNTPAIS, PA_ORIGEN
FROM         dbo.VPEDIMPPAIS
GROUP BY PI_CODIGO, MA_GENERICO, PA_ORIGEN

























GO
