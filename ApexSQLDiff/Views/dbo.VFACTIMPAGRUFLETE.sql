SET ANSI_NULLS ON
SET QUOTED_IDENTIFIER ON
GO








CREATE VIEW dbo.VFACTIMPAGRUFLETE
with encryption as
SELECT     SUM(FI_FLETE) AS FIA_FLETE, SUM(FI_SEGURO) AS FIA_SEGURO, SUM(FI_EMBALAJE) AS FIA_EMBALAJE, FI_FACTAGRU
FROM         VFACTIMPFLETE
GROUP BY FI_FACTAGRU






GO
