SET ANSI_NULLS ON
SET QUOTED_IDENTIFIER ON
GO






































































CREATE VIEW dbo.VCHECAPERMISO
with encryption as
SELECT PE_CODIGO
FROM PERMISODET
WHERE PE_CODIGO =
        (SELECT MAX(PE_FECHA)
      FROM PERMISO)






































































GO
