SET ANSI_NULLS ON
SET QUOTED_IDENTIFIER ON
GO




























CREATE PROCEDURE dbo.SP_MAESTRO_DETALLE (@TQ_CODIGO INTEGER, @MA_INV_GEN CHAR(1))   as

SET NOCOUNT ON 
BEGIN
  SELECT *  FROM VMAESTRO_DETALLE
   WHERE TI_CODIGO IN (SELECT TI_CODIGO FROM RELTEMBTIPO WHERE TQ_CODIGO = @TQ_CODIGO) AND MA_INV_GEN = @MA_INV_GEN
END



























GO
