SET ANSI_NULLS ON
SET QUOTED_IDENTIFIER ON
GO




























CREATE PROCEDURE dbo.SP_GETCONFIGURATIEMPO (@CLAVEPED INTEGER, @TIPO INTEGER,  @TIEMPO INTEGER OUTPUT)   as

BEGIN
	SELECT @TIEMPO = COT_TIEMPO FROM CONFIGURATIEMPO 
		WHERE CP_CODIGO = @CLAVEPED
		AND TI_CODIGO = @TIPO
END



























GO
